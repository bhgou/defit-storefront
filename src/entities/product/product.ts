export type Product = {
    id: string;
    name: string;
    price: number;
    imageUrl: string;
}

export type CreateProductResponse = {
    name: string;
    price: number;
    imageUrl: string;
}

